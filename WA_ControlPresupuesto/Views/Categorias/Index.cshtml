@model PaginacionRespuesta<Categoria>
@{
    ViewData["Title"] = "Categorias";
}
<h1>@ViewData["Title"]</h1>
<h5>La categoria nos permite indicar en qué gastamos nuestro dinero, 
    también, de dónde provienen nuestros ingresos.
</h5>@* 
<h2>Cantidad de categorías: @Model.CantidadTotalRecords -  Cantidad de Páginas @Model.CantidadTotalPaginas</h2> *@
<a asp-action="Crear" class="btn btn-primary mb-3">Crear Nuevo</a>

<div class="row">
    <label>Cantidad de Registros a mostrar:</label>
    <div class="mb-3 col-auto">
        <select asp-for="@Model.RecordsPorPagina" id="cantidadRecordsPorPagina" class="form-select">
            <option>3</option>
            <option>5</option>
            <option>10</option>
            <option>25</option>
        </select>
    </div>
</div>
<partial name="_Paginacion" />

<table class="table table-striped">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Tipo</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var categoria in Model.Elementos)
        {
            <tr>
                <td>@categoria.Nombre</td>
                <td>
                    @categoria.TipoOperacionId.ToString()
                </td>
                <td>
                    <a asp-action="Editar" asp-route-id="@categoria.Id" class="btn btn-primary">Editar</a>
                    <a asp-action="Borrar" asp-route-id="@categoria.Id" class="btn btn-danger">Borrar</a>
                </td>     
            </tr>

         }
    </tbody>
</table>
@section Scripts {
    <script>
        $("#cantidadRecordsPorPagina").change(function(){
            const recordsPorPagina = $(this).val();
            location.href = `/categorias/?pagina=1&recordsPorPagina=${recordsPorPagina}`;
        })
        // const select = document.querySelector("#cantidadRecordsPorPagina");
        // select.addEventListener("change", function () {
        //     window.location = `?pagina=1&recordsPorPagina=${select.value}`;
        // });
    </script>
}